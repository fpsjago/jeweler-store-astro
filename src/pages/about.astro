---
import Base from '../layouts/Base.astro';
import Newsletter from '../components/Newsletter.astro';
import { store } from '../data/store';
---

<Base title="About" currentPage="/about">
  <section class="about-hero">
    <div class="container">
      <h1>Our Story</h1>
      <p>For over three decades, {store.name} has been crafting jewelry that transcends trends and becomes part of your personal legacy.</p>
    </div>
  </section>

  <section class="container about-section">
    <div class="about-grid">
      <div class="about-img">💎</div>
      <div class="about-content">
        <h2>The Art of Craftsmanship</h2>
        <p>Every piece in our collection is handcrafted by master artisans who bring decades of expertise to their craft. We source only the finest materials — conflict-free diamonds, ethically mined gemstones, and recycled precious metals.</p>
        <p>Our atelier in New York combines traditional goldsmithing techniques with modern precision, ensuring each creation meets the highest standards of quality and beauty.</p>
      </div>
    </div>
  </section>

  <section class="container">
    <div class="values-grid">
      <div class="value-card">
        <div class="value-icon">🌿</div>
        <h3>Sustainable Luxury</h3>
        <p>Committed to ethical sourcing and sustainable practices. Every gemstone is traceable, and we use 100% recycled precious metals.</p>
      </div>
      <div class="value-card">
        <div class="value-icon">✋</div>
        <h3>Handcrafted Excellence</h3>
        <p>Each piece passes through the hands of master artisans who spend hundreds of hours perfecting every detail.</p>
      </div>
      <div class="value-card">
        <div class="value-icon">💝</div>
        <h3>Lifetime Guarantee</h3>
        <p>We stand behind every creation with a lifetime warranty on craftsmanship and complimentary annual maintenance.</p>
      </div>
    </div>
  </section>

  <Newsletter />
</Base>

<style>
  .about-hero { padding: var(--space-4xl) 0; text-align: center; background: var(--color-bg-alt); }
  .about-hero h1 { font-size: clamp(2rem, 5vw, 3.5rem); margin-bottom: var(--space-md); }
  .about-hero p { color: var(--color-text-muted); max-width: 600px; margin: 0 auto; font-size: 1.1rem; line-height: 1.8; }
  .about-section { padding: var(--space-3xl) 0; }
  .about-grid { display: grid; grid-template-columns: 1fr; gap: var(--space-2xl); align-items: center; }
  @media (min-width: 768px) { .about-grid { grid-template-columns: 1fr 1fr; } }
  .about-img { aspect-ratio: 4/3; background: var(--color-bg-alt); border-radius: var(--border-radius-lg); display: flex; align-items: center; justify-content: center; font-size: 4rem; }
  .about-content h2 { font-size: 1.8rem; margin-bottom: var(--space-md); }
  .about-content p { color: var(--color-text-muted); line-height: 1.8; margin-bottom: var(--space-md); }
  .values-grid { display: grid; grid-template-columns: 1fr; gap: var(--space-lg); padding: var(--space-3xl) 0; }
  @media (min-width: 768px) { .values-grid { grid-template-columns: repeat(3, 1fr); } }
  .value-card { text-align: center; padding: var(--space-xl); }
  .value-icon { font-size: 2.5rem; margin-bottom: var(--space-md); }
  .value-card h3 { font-size: 1.2rem; margin-bottom: var(--space-sm); }
  .value-card p { color: var(--color-text-muted); font-size: 0.9rem; line-height: 1.7; }
</style>
